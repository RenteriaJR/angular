import { ConvertPokeNameToHexaPipe } from './convert-poke-name-to-hexa.pipe';

describe('ConvertPokeNameToHexaPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertPokeNameToHexaPipe();
    expect(pipe).toBeTruthy();
  });
});
